# 项目简介
当前项目是一套完整的装备软件Mock平台，用于Mock装备软件中各种与硬件相关的函数返回值，解决装备软件需要依赖各种硬件才能运行的痛点问题，实现无硬件调试运行

# 功能
## 基于Java Agent的非侵入式Mock
基于Java Agent技术来实现装备Java代码的Mock

### 核心设计
1. 基于ByteBuddy+ Boostrap级Spy桥接类 + boostrap和agent双层jar包隔离，解决Java Agent依赖对宿主应用的污染和类隔离后跨类加载器的双向通信难题
2. 插件化设计，通过使用合理的Java插件类库来实现不同Mock模块的编写、引入、卸载，如模块A的所有Mock类放在一个java模块中，模块B的又放在另外的模块里面，通过provide作用域引入agent jar来导入对应的关键依赖，每个模块要能支持自测试
3. 支持配置，提供配置中心功能，配置功能主要服务于函数的Mock返回值，支持多种匹配方式(FixedValue固定值, FullMatch参数完全匹配, PatternMatch参数正则匹配)。支持配置组功能，一套配置组中有多个预设好的配置，配置中心可以在多个配置组中间进行切换(这边的设置要考虑好怎么跟工作台一起联动)，配置全部通过json文件来保存。Java通过文件监听来实时获取配置文件的变更并刷新进内存。来注意只能提供配置中心的这个功能，具体函数Mock要使用配置还是写死的Mock逻辑以用户编写的代码为准。举例一个配置组包含的内容：一个配置组中有多个文件，每个文件对应一个小分组(如机柜相关的Mock)，文件内容中包含这个小分组中的所有函数的详细mock配置

### 技术栈要求
1. 基于JDK 1.8, jdk路径为D:\Java\jdk1.8.0_281\bin\java.exe
2. 使用byte-buddy 1.9.16这个库来做字节码增强
3. 程序运行在windows系统上


## 桌面端工作台
强大完整的桌面端工作台，核心功能是进行Mock插件模块的管理(包含导入、启用、停用)、Mock的配置管理(支持切换、新增、复制、删除配置组，支持各种Mock参数的设置)

### 核心设计
1. 这个工作台是独立的，用于用户方便操作的一个工具，跟Agent相关的配置，包括插件模块、Mock配置管理在没有这个工作台的情况下也能正常独立运行，Java Agent对工作台没有依赖，全部通过json文件来管理，工作台来方便人设置json文件，Java Agent监听json文件并实时应用
2. 选定一个配置组后，自动识别所有小分组(单json文件)，界面工具在修改Mock时打开的是一个小分组中的内容，这样界面上的内容不会太多也更聚焦，然后支持在侧边切换其余的小分组，修改后需要点击按钮或者ctrl+s保存使json文件应用修改。配置修改的组件依据Mock的匹配方式来使用不同的组件，并且整个json文件支持使用monaco editor来修改

### 技术栈要求
1. 使用Electron 43.1.0
2. 使用React 19 + React Compiler + Vite + Vitest + Oxc + Tailwindcss V4
3. json编辑器使用monaco editor，编译后内嵌资源，不使用cdn或者后续下载
